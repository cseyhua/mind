package main

import filecase "cseyhua/file_case"

func main() {
	// defer_case.DeferCase()
	// defercase.ExceptionHandleCase()
	// channelcase.Communication()
	// channelcase.ConcurrentSync()

	// ch := make(chan os.Signal, 1)
	// signal.Notify(ch, os.Interrupt)
	// <-ch
	filecase.CopyDirToDir()
}
